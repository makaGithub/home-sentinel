# ==========================================
# Home Sentinel Configuration
# ==========================================
# Скопируйте этот файл в .env и заполните необходимые значения
# cp .env.example .env

# ==========================================
# Deploy Configuration
# ==========================================
# Удаленный хост для деплоя
# REMOTE_HOST=your-server.com
# REMOTE_USER=your-user
# REMOTE_PATH=/path/to/home-sentinel

# Метод деплоя: git или rsync
# DEPLOY_METHOD=git

# SSH опции
# SSH_OPTS=-o StrictHostKeyChecking=no

# Копировать .env файл при деплое
# COPY_ENV=true

# Собирать на удаленном хосте
# BUILD_REMOTE=false

# Перезапускать сервисы после деплоя
# RESTART_SERVICES=true

# ==========================================
# Documentation Generation Configuration
# ==========================================
# Путь для сохранения временных артефактов (DOCX, PPTX)
BUILD_DIR=build

# Путь для сгенерированного HTML (для GitHub Pages)
DOCS_BUILD_DIR=docs/_build/html

# Директория с исходными markdown файлами
DOCS_SOURCE_DIR=docs/source

# Шаблон имени файла для сгенерированных документов
# Используется strftime для даты и времени
# Доступные переменные: {source_name}, {timestamp}, {date}, {time}
# Примеры:
#   {source_name}_{timestamp} -> project_20241215_143022.docx
#   {source_name}_{date} -> project_20241215.docx
#   {source_name} -> project.docx (без даты)
DOC_NAME_TEMPLATE={source_name}_{timestamp}

# Формат timestamp (для strftime)
# По умолчанию: %Y%m%d_%H%M%S -> 20241215_143022
TIMESTAMP_FORMAT=%Y%m%d_%H%M%S

# Формат date (для strftime)
# По умолчанию: %Y%m%d -> 20241215
DATE_FORMAT=%Y%m%d

# Формат time (для strftime)
# По умолчанию: %H%M%S -> 143022
TIME_FORMAT=%H%M%S

# ==========================================
# GitHub Pages Configuration
# ==========================================
# Ветка для GitHub Pages (не используется напрямую, используется Actions Deploy)
# GH_PAGES_BRANCH=gh-pages

# Автоматически пушить при использовании --publish (локально)
# GH_PAGES_AUTO_PUSH=true

# ==========================================
# GitHub Releases Configuration
# ==========================================
# Префикс для тегов Release документации
# Пример: docs-v1.0.0, docs-v20241215
GITHUB_RELEASE_TAG_PREFIX=docs-v

# ==========================================
# Source Files Configuration
# ==========================================
# Исходные markdown файлы (пути относительно корня проекта)
DOCS_PROJECT_MD=docs/source/project.md
DOCS_PRESENTATION_MD=docs/source/presentation.md
