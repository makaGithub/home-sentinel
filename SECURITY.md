# Security Policy

## 🔒 Политика безопасности

### Чувствительные данные

Следующие данные **НИКОГДА** не должны попадать в git репозиторий:

- **Пароли** - любые пароли (баз данных, API, пользователей)
- **Токены** - API токены, токены доступа (Immich, Home Assistant, etc.)
- **Ключи** - SSH ключи, API ключи, приватные ключи
- **IP адреса** - реальные IP адреса серверов и устройств
- **Имена пользователей** - реальные имена пользователей для доступа к серверам
- **Пути к серверам** - реальные пути на удаленных хостах

### Файлы, которые НЕ должны быть в репозитории

- `.env` - локальный файл с реальными настройками
- `*.key` - приватные ключи
- `*.pem` - сертификаты и ключи
- `id_rsa*` - SSH ключи
- Любые файлы с паролями или токенами

### Как правильно работать с конфигурацией

1. **Используйте `.env.example`** как шаблон
2. **Создайте локальный `.env`** на основе `.env.example`:
   ```bash
   cp .env.example .env
   # Отредактируйте .env с вашими реальными данными
   ```
3. **Никогда не коммитьте `.env`** - он уже в `.gitignore`

### Проверка перед коммитом

Перед каждым коммитом проверьте:

```bash
# Проверьте, что .env не попал в staged
git status | grep .env

# Проверьте staged изменения на наличие чувствительных данных
git diff --cached | grep -E "(PASSWORD|TOKEN|SECRET|API_KEY|172\.|192\.|10\.)"
```

### Если вы случайно закоммитили чувствительные данные

1. **НЕМЕДЛЕННО** отзовите токены/пароли
2. Удалите данные из истории git (если еще не запушено):
   ```bash
   git reset HEAD~1  # Если последний коммит
   # Или используйте git filter-branch или BFG Repo-Cleaner
   ```
3. Если уже запушено - смените все пароли/токены

### Защита .env файла

Файл `.env` уже в `.gitignore`, но убедитесь:

```bash
# Проверьте, что .env игнорируется
git check-ignore .env

# Должно вывести: .env
```

### Дополнительная защита

В проекте настроен pre-commit hook, который автоматически проверяет:
- Попытки закоммитить `.env` файлы
- Наличие чувствительных данных в staged изменениях

## 📞 Сообщить о проблеме безопасности

Если вы обнаружили утечку чувствительных данных в репозитории:
1. Немедленно смените все пароли/токены
2. Отзовите все токены доступа
3. Создайте issue для удаления данных из истории (если нужно)

